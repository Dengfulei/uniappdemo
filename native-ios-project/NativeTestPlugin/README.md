# NativeTestPlugin

`NativeTestPlugin` is a tiny Swift framework used to verify the recommended uni-app iOS native integration path: package plugin code as a framework, embed it in the native host project, then call it from native Swift/Objective-C code.

## Build

From the repository root:

```sh
sh native-ios-project/NativeTestPlugin/build-xcframework.sh
```
